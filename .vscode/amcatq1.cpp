#include <iostream>
using namespace std;

int main() {
    int N, K;
    cin >> N >> K;

    int arr[N];

    for(int i = 0; i < N; i++) {
        cin >> arr[i];
    }

    for(int i = 0; i <=K; i++) {
        int found = 0;
        
        }

        if(found == 0)
            cout << 0 << " ";
    }

    return 0;
}